#!/usr/bin/python3
"""Module that defines a Square with size."""

class Square:
    """Represents a square with a private size attribute."""

    def __init__(self, size):
        """Initializes the square with a given size."""
        self.__size = size
