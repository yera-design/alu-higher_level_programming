-- This script displays the number of records with id = 89 in the first_table
-- The database name is passed as an argument

-- Count records where id = 89
SELECT COUNT(*) 
FROM first_table 
WHERE id = 89;
