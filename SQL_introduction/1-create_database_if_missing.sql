-- This script creates the database hbtn_0c_0 in MySQL
-- It ensures the script does not fail if the database already exists

-- Create database only if it does not exist
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
